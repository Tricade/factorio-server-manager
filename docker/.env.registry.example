# Published GitHub Container Registry image for this repository.
FSM_IMAGE=ghcr.io/tricade/factorio-server-manager:latest

FSM_HTTP_PORT=8080
FACTORIO_PORT=34197
FACTORIO_VERSION=latest
FSM_COOKIE_SECURE=false
# Leave empty to generate and persist a random localhost-only RCON password.
RCON_PASS=
